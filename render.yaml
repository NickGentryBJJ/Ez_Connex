databases:
  - name: EZ Connex
    user: EZ Connex
    plan: free
    region: oregon

services:
  - type: web
    name: EZ Connex
    plan: free
    region: oregon
    branch: main
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: EZ Connex
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false